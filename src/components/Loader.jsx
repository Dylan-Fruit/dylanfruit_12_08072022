import React from "react";

/**
 * Loader component
 * @returns {JSX}
 */

const Loader = () => {
  return (
    <div className="main-loading">
      <span className="loader"></span>
      <p>Loading...</p>
    </div>
  );
};

export default Loader;
